import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clutchupapp.clutchup',
  appName: 'ClutchUpApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
